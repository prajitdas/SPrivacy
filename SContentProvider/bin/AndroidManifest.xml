<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.prajitdas.contentprovider"
    android:versionCode="1"
    android:versionName="1.0" >

<!--
	Permissions need to be included in the future     
	<permission android:name="com.prajitdas.contentprovider.permission.DEADLY_ACTIVITY"
        android:label="@string/permlab_deadlyActivity"
        android:description="@string/permdesc_deadlyActivity"
        android:permissionGroup="android.permission-group.COST_MONEY"
        android:protectionLevel="dangerous" /> -->
    
    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- Required permission not required at or below Android 4.3 API level -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- Unusual but true! -->
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        
		<provider android:name="SContentProvider" 
			android:authorities="com.prajitdas.contentprovider"
			android:exported="true" >
<!-- 			android:permission="com.prajitdas.contentprovider.permission." > -->
		</provider>
    </application>

</manifest>